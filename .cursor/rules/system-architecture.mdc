---
description: Always respect the system architecture, AI reuse rules, and module extension-first development.
alwaysApply: true
---

# System Architecture Rules

Always follow the architecture defined in `SYSTEM_ARCHITECTURE.md`.

## Stack

- Frontend: React + TypeScript
- Backend: FastAPI
- Database: SQL Server
- AI: LangChain-based generation services

## Backend Rules

The backend architecture is:

`Controller -> Service -> Repository`

Apply these rules:

- Controllers expose API endpoints and handle request/response concerns only.
- Services contain business logic, orchestration, validation, and feature workflows.
- Repositories interact with SQL Server and encapsulate persistence access.
- Do not put business logic in controllers.
- Do not query the database directly from controllers.
- Do not bypass services when implementing feature behavior.
- Prefer extending an existing controller, service, or repository before creating a new one.
- Do not create duplicate services or repository flows that solve the same problem.

## AI Rules

AI generation logic is a shared capability and must be reusable across features.

Apply these rules:

- Keep LangChain orchestration in reusable AI or service layers.
- Reuse existing AI generation services whenever possible.
- Do not duplicate prompts, chains, parsing logic, or generation workflows across features when shared behavior is possible.
- Controllers must not contain AI workflow logic.
- Feature services may call shared AI generation components.
- Prefer extending existing AI modules before introducing new AI services.
- AI artifacts must follow the review lifecycle defined in `ai_review_workflow.md` when that reference is available in the repository.

## Frontend Rules

The frontend architecture is:

`Primitive -> Semantic -> Feature`

Apply these rules:

- Primitive components are generic UI building blocks.
- Semantic components are domain-specific UI elements built from primitives.
- Feature modules implement end-to-end user workflows.
- Do not place feature workflow logic in primitive components.
- Do not make primitives depend on feature modules.
- Keep domain meaning in semantic components and workflow orchestration in features or pages.
- Prefer extending an existing primitive, semantic component, or feature module before creating a new one.
- Do not create duplicate components when an existing module can be extended safely.

## Decision Guidance

When making changes:

1. Place code in the narrowest correct layer.
2. Prefer reuse over duplication.
3. Preserve dependency direction from higher-level orchestration to lower-level reusable layers.
4. Prefer extending existing modules, services, and components over creating parallel implementations.
5. If a change conflicts with these rules, refactor toward the architecture instead of adding exceptions unless a documented constraint requires it.
