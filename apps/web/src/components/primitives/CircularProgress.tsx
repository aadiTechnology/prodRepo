/**
 * CircularProgress — UI Primitive
 * Re-export of MUI CircularProgress for loading states (e.g. SaveButton).
 */

import { CircularProgress as MuiCircularProgress, CircularProgressProps as MuiCircularProgressProps } from "@mui/material";

export type CircularProgressProps = MuiCircularProgressProps;
export default MuiCircularProgress;
