/**
 * FormControlLabel — UI Primitive
 * Re-export of MUI FormControlLabel for label + control composition (e.g. checkbox with label).
 */

import { FormControlLabel as MuiFormControlLabel, FormControlLabelProps } from "@mui/material";

export type { FormControlLabelProps };
export const FormControlLabel = MuiFormControlLabel;
