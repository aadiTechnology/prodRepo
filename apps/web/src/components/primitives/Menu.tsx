/**
 * Menu — UI Primitives
 * Single point of access to MUI Menu.
 */

import { Menu as MuiMenu } from "@mui/material";

export type MenuProps = React.ComponentProps<typeof MuiMenu>;

export const Menu = MuiMenu;

