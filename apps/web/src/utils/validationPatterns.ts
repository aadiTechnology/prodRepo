/** Shared regex for form validation `pattern` rules. */
export const EMAIL_PATTERN = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
export const PHONE_PATTERN = /^\d{10,15}$/;
export const NUMERIC_PATTERN = /^\d+$/;
